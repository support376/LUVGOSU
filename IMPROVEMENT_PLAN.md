# LuvOS MVP 개선 계획서

> 현재 상태: MVP 완성, 결제 연동 완료, 라이브 배포 중
> 작성일: 2026-04-03
> URL: https://luvos-mvp.vercel.app

---

## 1. 보안 (즉시 수정 필요)

### 크레딧 조작 가능
- 현재 쿠키 기반 → 브라우저 개발자도구로 `luvos_credits=999` 설정 가능
- **해결**: 서버 DB (Supabase 등)로 이전, 유저 인증 연동

### 결제 승인 검증 미흡
- 결제 success 페이지에서 서버 승인은 호출하지만, 금액/orderId 위변조 검증 강화 필요
- **해결**: orderId를 서버에서 미리 생성하고 매칭 검증

### Claude 응답 XSS 위험
- ScoreReport에서 Claude 출력을 그대로 렌더링 → 악의적 HTML 삽입 가능성
- **해결**: DOMPurify로 sanitize 또는 텍스트 전용 렌더링

### API 레이트 리밋 없음
- /api/simulate, /api/score 무제한 호출 가능 → API 비용 폭탄
- **해결**: IP 기반 레이트 리밋 (Vercel Edge Middleware 또는 Upstash Ratelimit)

---

## 2. 비즈니스 모델

### 전환율 개선
- 무료 1회 후 이탈률 높을 것으로 예상
- **해결**: 무료 세션 완료 후 "이 유형과 다른 시나리오도 해보세요" 업셀 모달
- 리포트에 "프리미엄에서만 볼 수 있는 심층 분석" 티저 추가

### 구독 모델 검토
- 현재: 건당 결제 (4,900원/1회, 17,900원/5회)
- **검토**: 월 9,900원 무제한, 연 99,000원 등 구독 추가

### 프리미엄 콘텐츠
- 일부 시나리오를 "잠금" 처리 (잠자리, 신뢰 등 민감 시나리오)
- 프리미엄 전용 심층 리포트 (대화 패턴 시계열 분석, PDF 다운로드)

### 레퍼럴 프로그램
- "친구 초대하면 2회 무료" 등 바이럴 유도

---

## 3. 유저 경험 (UX)

### 결제 위젯 로딩 속도
- iframe 로딩까지 3~5초 대기 → 사용자 이탈 가능
- **해결**: 로딩 중 스켈레톤 UI, 예상 시간 표시

### 세션 기록 없음
- 시뮬레이션 완료 후 다시 볼 수 없음
- **해결**: DB에 대화 + 채점 결과 저장, 마이페이지에서 조회

### 채점 대기 시간
- Claude API 호출로 10~30초 소요, 사용자가 기다림
- **해결**: 예상 시간 표시, 진행률 바, 팁 텍스트 롤링

### 대화 중 힌트 기능 없음
- 점수가 낮을 때 실시간 도움 없음
- **해결**: 3턴 연속 독소 사용 시 "이렇게 말해보는 건 어떨까요?" 팝업

### 재도전 시 비교 불가
- 같은 시나리오 재도전해도 이전 점수와 비교 안 됨
- **해결**: "지난번 B등급 → 이번 A등급" 비교 화면

---

## 4. 기술 부채

### 데이터베이스 없음
- 모든 상태가 쿠키 → 브라우저 바뀌면 초기화
- **해결**: Supabase (무료 tier) 연동 — users, sessions, scores 테이블

### 유저 인증 없음
- 계정 없이 쿠키만 → 기기 바뀌면 크레딧 사라짐
- **해결**: 카카오/구글 소셜 로그인 (NextAuth.js)

### API 입력값 검증 없음
- coordinate, situationId 등 미검증으로 전송
- **해결**: zod 스키마 검증 추가

### 에러 모니터링 없음
- 프로덕션 에러 추적 불가
- **해결**: Sentry 무료 tier 연동

### Claude 모델 버전 하드코딩
- `claude-sonnet-4-20250514` 직접 명시
- **해결**: 환경변수로 추출

---

## 5. 콘텐츠 & 채점 깊이

### 채점 피드백 구체성 부족
- "감정 반응성이 낮습니다" → 어떤 발화에서 낮았는지 불명확
- **해결**: "턴 5에서 '그래서 결론이 뭔데?'라고 한 부분이 감정 무시로 판정" 식 구체화

### 대화 흐름 채점 없음
- 각 발화를 독립 평가, 전체 흐름(에스컬레이션→디에스컬레이션) 미반영
- **해결**: 채점 프롬프트에 "대화 흐름 변화 추적" 항목 추가

### 커스텀 시나리오 없음
- 유저가 직접 상황 입력/실제 카톡 대화 붙여넣기 불가
- **해결**: 텍스트 입력 → Claude가 Situation 형태로 변환 후 시뮬 시작

### 성격 유형 리포트 없음
- 여러 세션 데이터를 종합한 "나의 연애 커뮤니케이션 유형" 분석
- **해결**: 5회 이상 완료 시 종합 유형 리포트 생성

---

## 6. SEO & 마케팅

### 메타 태그 부족
- Open Graph, Twitter Card 없음 → SNS 공유 시 미리보기 없음
- **해결**: layout.tsx에 OG 태그 추가

### 검색 최적화 안 됨
- 시나리오가 쿼리 파라미터 (`?s=S1`) → 구글 인덱싱 불가
- **해결**: `/scenarios/읽씹` 같은 정적 라우트 생성

### 랜딩 페이지 카피 부족
- "연애력을 데이터로 만든다" 한 줄만 → 왜 필요한지, 어떻게 도움되는지 설명 없음
- **해결**: 문제→해결→증거 구조의 카피 작성

### 블로그/콘텐츠 없음
- "연애 갈등 해결법", "Gottman Method란?" 등 SEO 콘텐츠 없음
- **해결**: /blog 라우트 + 5개 이상 글 작성

---

## 7. 분석 & 데이터

### 이벤트 트래킹 제로
- 유저 행동 추적 없음 — 어디서 이탈하는지 모름
- **해결**: PostHog 또는 Mixpanel 연동

### 퍼널 분석 불가
- 홈→프로필→좌표→목표→상황→시뮬→완료→결제 중 어디서 빠지는지?
- **해결**: 각 단계별 이벤트 발화

### API 비용 추적 없음
- Claude API 호출당 비용, 일일 총 비용 모니터링 없음
- **해결**: API 호출 로깅 + 일일 리포트

---

## 8. 모바일 최적화

### Safari iframe 이슈
- iOS Safari에서 토스 위젯 iframe 높이 감지 불안정
- **해결**: ResizeObserver 사용, iOS 전용 폴백

### 키보드 올라올 때 입력 가림
- 채팅 입력 시 모바일 키보드가 입력창을 가릴 수 있음
- **해결**: `visualViewport` API로 키보드 높이 감지, 스크롤 조정

### PWA 미적용
- 홈 화면 추가 시 앱 느낌 없음
- **해결**: manifest.json + 서비스워커 추가

---

## 우선순위 로드맵

### Phase 1: 안정화 (1주)
- [ ] 크레딧 조작 방지 (서버 검증 강화)
- [ ] API 레이트 리밋
- [ ] Claude 출력 sanitize
- [ ] 에러 모니터링 (Sentry)
- [ ] 이벤트 트래킹 (PostHog)

### Phase 2: 전환율 개선 (2주)
- [ ] 세션 완료 후 업셀 모달
- [ ] 리포트에 프리미엄 티저
- [ ] OG 메타 태그 + SNS 공유
- [ ] 결제 위젯 로딩 UX 개선

### Phase 3: 사용자 고착 (3~4주)
- [ ] Supabase 연동 (유저, 세션, 점수)
- [ ] 소셜 로그인 (카카오)
- [ ] 세션 히스토리 조회
- [ ] 점수 비교 (이전 vs 현재)
- [ ] 종합 유형 리포트

### Phase 4: 성장 (5~8주)
- [ ] 커스텀 시나리오 (카톡 대화 붙여넣기)
- [ ] 프리미엄 시나리오 잠금
- [ ] 구독 모델 추가
- [ ] 블로그 SEO 콘텐츠
- [ ] 레퍼럴 프로그램
- [ ] PWA 적용
